# PhoneHub 路线图与新测试项目：实时地图（LiveMap）

> 2026-09-13 起草。先给结论，再给分阶段路线，最后是新测试项目 `cs_apps/LiveMap/` 的设计说明。

---

## 一、新测试项目：LiveMap（手机实时位置 → 电脑地图）

### 为什么选它
- PhoneHub 已有定位能力（`locationservice.kt`、路线图页、`location_batch` 上报），但电脑端**没有一张真正的地图**，数据只是数字坐标。
- 做一张"手机在哪、去过哪"的实时地图，是定位功能的自然出口，也验证"手机采集 → PC 展示"的实时通道。

### 架构（与 audio_web_player 同款套路：PC 起服务，手机/浏览器连）
```
手机 App(Kotlin, FusedLocation/GPS)
   │  HTTP POST /push {lat, lon, spd, bat, ts}（1~2s 一次，或位移>10m 才发）
   ▼
PC: cs_apps/LiveMap/pc/map_server.py（纯标准库 http.server，端口 5678）
   ├─ GET /            → Leaflet 地图页（内嵌 HTML，无构建工具）
   ├─ GET /track       → SSE 或轮询 JSON（最近轨迹点）
   └─ POST /push       → 手机推点（存内存 deque + 磁盘 CSV）
电脑浏览器打开 http://<PC IP>:5678/ 即可看实时位置与轨迹。
```

### 分工（按用户要求）
- **手机端：Kotlin**。`android/LiveLocationPusher.kt`：一个前台服务（`foregroundServiceType="location"`），GPS/网络定位 → POST 给 PC；断网缓存到本地、恢复后续传；通知显示"实时位置共享中"。
- **电脑端：网页（Leaflet + OpenStreetMap 瓦片）**。零依赖、比 tkinter 画地图简单一个量级；后续要"像样窗口"再套 PyQt WebEngine 或直接继续用浏览器。

### 目录
```
cs_apps/LiveMap/
├── pc/map_server.py      # 零依赖 HTTP 服务 + 地图页（本期交付，可独立运行）
├── android/LiveLocationPusher.kt   # 手机端前台服务（参考实现，验证后并入主 app）
└── README.md
```

### 里程碑
- M1（本期）：PC 网页地图 + 手动模拟点/真实手机推点，轨迹 polyline + 当前点 marker + 速度/电量气泡。
- M2：手机端服务并入 PhoneHub（路线图页加开关），支持历史轨迹回放（按时间轴拖动）。
- M3：地理围栏（进入/离开区域给 PC 弹通知）、多点手机同屏。

---

## 二、主线 Roadmap（结合《优化.md》与本次改动）

### P0 稳定收口（本周）
- [x] 互斥：投屏试听 ⇄ 电脑声音收听，二选一（本次已做，防啸叫）。
- [x] PC→手机传文件卡「已暂停(对端)」（本次已做）。
- [x] 通知「打开应用并投屏」打通 + 弹窗预构建（本次已做）。
- [x] 剪贴板手机端常同步（本次已做）。
- [x] 远程文件管理去多选（本次已做）。
- [ ] 用户回归：投屏 5 分钟 / 电脑声音 / 文件互传 / 剪贴板 四项冒烟。

### P1 性能与安全（下个迭代，对应《优化.md》）
1. 5423/5435 鉴权补齐；默认 token 首次随机化。
2. `/upload` 改 `cv2.imdecode`（省一半 CPU）；录制收尾 ffmpeg 移出锁外。
3. `pageCache` LRU；主线程位图解码下放。
4. 轮询→推送：消息+帧合一条 WebSocket（最大单项优化，先做消息与状态，再做帧）。

### P2 功能扩展（1~2 月）
1. **LiveMap 并入主程序**（路线图页：开关 + 地图链接/二维码）。
2. 电脑声音做成 `mediaPlayback` 前台服务（彻底解决 EMUI 后台冻结）。
3. 通知页：远程快捷回复、通知搜索、按应用分组。
4. 文件管理：双向传输队列可视化、断点续传 UI、缩略图。

### P3 工程化（持续）
1. 把冒烟脚本固化进 `desktop/tests/`，协议改动先跑测试。
2. `架构.md`：端口表 / 命令表 / 数据目录 / 通道图。
3. requirements 锁版本；desktop 拆包（connection_manager 逐步瘦身）。

### 风险与依赖
- 华为/荣耀后台冻结：所有"常驻"功能（定位共享、电脑声音）最终都要前台服务 + 电池白名单引导。
- Leaflet 瓦片需要外网；内网环境准备离线瓦片兜底（M2 再说）。
