# PhoneHub 路线图与新测试项目：实时地图（LiveMap）

> 2026-09-13 起草。先给结论，再给分阶段路线，最后是新测试项目 `cs_apps/LiveMap/` 的设计说明。

---

## 一、新测试项目：LiveMap（手机实时位置 → 电脑地图）

### 为什么选它
- PhoneHub 已有定位能力（`locationservice.kt`、路线图页、`location_batch` 上报），但电脑端**没有一张真正的地图**，数据只是数字坐标。
- 做一张"手机在哪、去过哪"的实时地图，是定位功能的自然出口，也验证"手机采集 → PC 展示"的实时通道。

### 架构（与 audio_web_player 同款套路：PC 起服务，手机/浏览器连）
```
手机 App(Kotlin, FusedLocation/GPS)
   │  HTTP POST /push {lat, lon, spd, bat, ts}（1~2s 一次，或位移>10m 才发）
   ▼
PC: cs_apps/LiveMap/pc/map_server.py（纯标准库 http.server，端口 5678）
   ├─ GET /            → Leaflet 地图页（内嵌 HTML，无构建工具）
   ├─ GET /track       → SSE 或轮询 JSON（最近轨迹点）
   └─ POST /push       → 手机推点（存内存 deque + 磁盘 CSV）
电脑浏览器打开 http://<PC IP>:5678/ 即可看实时位置与轨迹。
```

### 分工（按用户要求）
- **手机端：Kotlin**。`android/LiveLocationPusher.kt`：一个前台服务（`foregroundServiceType="location"`），GPS/网络定位 → POST 给 PC；断网缓存到本地、恢复后续传；通知显示"实时位置共享中"。
- **电脑端：网页（Leaflet + OpenStreetMap 瓦片）**。零依赖、比 tkinter 画地图简单一个量级；后续要"像样窗口"再套 PyQt WebEngine 或直接继续用浏览器。

### 目录
```
cs_apps/LiveMap/
├── pc/map_server.py      # 零依赖 HTTP 服务 + 地图页（可独立运行）
├── android/              # 独立 Gradle 工程（**不并入主工程**），包名 com.phonehub.livemap
│   ├── settings.gradle.kts / build.gradle.kts / gradle.properties / local.properties
│   └── app/src/main/…
│       ├── MainActivity.kt          # 控制台：填 IP/端口、起停、实时状态
│       └── LiveLocationPusher.kt    # 前台定位服务：推点 + 断网缓存补发
└── README.md
```
手机端刻意**零第三方依赖**（平台 LocationManager / Notification.Builder / HttpURLConnection），
因此本机离线（`--offline`）也能构建 —— Gradle 缓存里没有 androidx 的传递依赖。

### 里程碑
- ✅ **M1（已完成 2026-09-13）**：PC 网页地图 + 手动模拟点 + **真实手机推点**，
  轨迹 polyline + 当前点 marker + 速度/电量/精度气泡。
  手机端已做成**独立 APK**（包名 `com.phonehub.livemap`，与主 app 共存、互不干扰），
  `assembleDebug` 0 警告 0 错误并装机；真机端到端验证：手机前台服务 → 电脑端收到
  真实 GPS 点（36.70459,119.165365，精度 30m→17.5m 逐步收敛）。
- ⬜ **M2**：按需要再并入 PhoneHub 主 app（路线图页加开关），支持历史轨迹回放（时间轴拖动）。
- ⬜ **M3**：地理围栏（进入/离开区域给 PC 弹通知）、多点手机同屏。

> 用户明确要求：**这一测试项目单独打包，不合并进主工程代码**，所以 M2 暂不做。

---

## 二、主线 Roadmap（结合《优化.md》与本次改动）

### P0 稳定收口（本周）
- [x] 互斥：投屏试听 ⇄ 电脑声音收听，二选一（本次已做，防啸叫）。
- [x] PC→手机传文件卡「已暂停(对端)」（本次已做）。
- [x] 通知「打开应用并投屏」打通 + 弹窗预构建（本次已做）。
- [x] 剪贴板手机端常同步（本次已做）。
- [x] 远程文件管理去多选（本次已做）。
- [ ] 用户回归：投屏 5 分钟 / 电脑声音 / 文件互传 / 剪贴板 四项冒烟。

### P1 性能与安全（对应《优化.md》）
> 2026-09-13 夜逐条对着**当前代码**重新核验 —— 发现《优化.md》有多条在写完之后就已经被修掉了。
> 下表是核实后的真实状态（✅ 已具备 / ⬜ 仍待做）。

- ✅ **5423 投屏服务鉴权**（本轮新增）。原先全路由零鉴权：局域网内任何设备都能实时看手机屏幕
  （`/stream`）、向手机注入点击（`/remote_*`）、下载录像（`/output/`）。现在读 `desktop/settings.json`
  里同一个 `secret_token` 做 `before_request` 校验；手机端 5 处请求 + 桌面端全部本地调用方已补凭据。
  浏览器只需用 `http://<PC>:5423/live?token=<令牌>` 打开一次（会种同源 Cookie，页面内
  `/stream`、`/audio_stream`、`/output` 子请求自动带凭据）。已用 curl 验证 401/200。
- ✅ **5435 音频服务鉴权** —— 核验发现早已具备（日志里有 `[AUTH FAIL] 5435音频`）。
- ✅ `/upload` 一步解码 —— **已经是** `cv2.imdecode`（《优化.md》该条过时）。
- ✅ 录制收尾 ffmpeg 移出锁外 —— **已经是**后台线程合并（同上过时）。
- ✅ 桌面退出收尾录制 —— 已做（`stop_server` 收尾 + `_drain_finalizers` 限时等落盘）。
- ✅ 节拍器 1ms 空转 —— 已修（未录制 50ms 慢轮询 + 录制中按下一槽位精确等待）。
- ✅ 冲突重命名路径穿越 —— 已做 `os.path.basename` 清洗。
- ✅ MediaProjection「假持久化授权」—— 已删。
- ⬜ **默认 token 首次随机化**（`541881452418845` 仍硬编码在两端）。注意这条一旦做，手机端与
  桌面端必须**同时**换令牌，属于一次「重新配对」，需要你确认后我再动。
- ⬜ `pageCache` LRU（现只有零星 `remove`，没有淘汰策略，17 个页面常驻）。
- ⬜ 主线程位图解码下放（`decodeByteArray` 仍在主线程，封面/摄像头帧会卡 UI）。
- ⬜ WakeLock 策略（保活服务仍 `acquire(10 分钟)` 循环重持）。
- ⬜ **轮询 → 推送**（500ms 状态 / 16ms 帧 / 30ms 音频仍各自独立 HTTP；延迟、耗电、流量的最大单项）。
- ⬜ `/output` 扩展名白名单（可选加固；当前 `send_from_directory` 本身已防穿越）。

### P2 功能扩展（1~2 月）
1. **LiveMap 并入主程序**（路线图页：开关 + 地图链接/二维码）。
2. 电脑声音做成 `mediaPlayback` 前台服务（彻底解决 EMUI 后台冻结）。
3. 通知页：远程快捷回复、通知搜索、按应用分组。
4. 文件管理：双向传输队列可视化、断点续传 UI、缩略图。

### P3 工程化（持续）
1. 把冒烟脚本固化进 `desktop/tests/`，协议改动先跑测试。
2. `架构.md`：端口表 / 命令表 / 数据目录 / 通道图。
3. requirements 锁版本；desktop 拆包（connection_manager 逐步瘦身）。

### 风险与依赖
- 华为/荣耀后台冻结：所有"常驻"功能（定位共享、电脑声音）最终都要前台服务 + 电池白名单引导。
- Leaflet 瓦片需要外网；内网环境准备离线瓦片兜底（M2 再说）。
