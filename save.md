# 电脑-手机互联App 完整框架定稿

## 一、项目基础信息

### 设备环境
- **电脑**：2015年产 Intel i5（1.7GHz，4核4线程），8GB内存，固态硬盘，Ubuntu+Windows双系统
- **手机**：华为鸿蒙3系统（内核基于Android 10）

### 开发环境
- **开发工具**：Trae（VS Code魔改版），不安装Android Studio
- **编程语言**：手机端Kotlin，电脑端Python，服务器端Python（Flask）
- **打包**：暂不进行，延后处理

---

## 二、连接策略

### 通道优先级（从高到低）
1. USB数据线（ADB）
2. 路由器（WiFi直连）
3. ~~PAW中转（PythonAnywhere）~~ **（已禁用，代码全部注释）**

### 连接模式定义

| 模式 | 触发条件 | 通道 |
|------|---------|------|
| USB数据线 | USB插着 | ADB |
| 路由器WiFi | 无USB + 有缓存IP + 直连成功 | HTTP |
| PAW中转兜底 | 无USB + 无缓存 或 直连失败 | PythonAnywhere长轮询+分块传输 |

### USB连接自动升级
- USB插上后自动执行 `adb tcpip 5555`，开启无线ADB
- 之后画面和声音全走WiFi ADB，USB插拔无感切换

### 连接状态监测与自动升降级
- **升级**：新通道连续3次成功请求才切换
- **降级**：重试3次全失败立即降级
- **传输期间**：暂停通道切换探测，传完再恢复
- **重连**：断开后自动重连，先重试原方式3次，失败降级

### IP缓存逻辑
- 本地缓存的电脑IP仅在直连成功时更新
- 缓存IP直连失败不更新缓存

---

## 三、通信协议：JSON V3.0

### 统一结构
```json
{
  "token": "鉴权码",
  "activate": "update_ip / offline / send / get_cmd / get_msg",
  "data": {
    "action": "cmd / txt / send_file_head / file_complete / stop",
    "txt": "文本内容（可选）",
    "cmd": "指令内容（可选）",
    "ip": "IP地址（update_ip时）",
    "port": "端口号（update_ip时）",
    "timestamp": "时间戳（offline时）",
    "file_name": "文件名（send_file_head时）",
    "file_size": "文件大小（send_file_head时）",
    "file_id": "文件唯一标识",
    "chunk_size": "块大小（PAW模式，64KB）",
    "total_parts": "总块数（PAW模式）",
    "part_num": "当前块序号"
  }
}
```

### activate枚举值
| 值 | 用途 | 方向 |
|----|------|------|
| update_ip | 电脑向PAW上报局域网IP | 电脑→PAW |
| offline | 电脑发送关机标记 | 电脑→PAW |
| send | 发送指令/文本/文件头/完成通知 | 双向→PAW |
| get_cmd | 电脑长轮询拉取指令 | 电脑→PAW |
| get_msg | 手机拉取电脑主动发的消息 | 手机→PAW |

### data.action枚举值
| 值 | 用途 |
|----|------|
| cmd | 执行系统指令 |
| txt | 显示文本消息 |
| send_file_head | 发送文件头信息 |
| file_complete | 文件全部上传完成通知 |
| stop | 暂停或取消传输 |

---

## 四、文件传输引擎（功能1）

### 基础规则
| 规则项 | 内容 |
|--------|------|
| 方向 | 手机↔电脑 |
| 通道优先级 | USB数据线 > 路由器WiFi > PAW中转 |
| 并发限制 | 全局同时只传1个文件，传完才能开始下一个 |
| 进度显示 | 百分比 + 预计剩余时间 |
| 安全限制 | 无，什么文件都传，不做类型判断 |
| 完成确认 | 接收方发`file_complete` → 发送方回ack → 10秒没收到重发 |
| 校验 | 两端对比文件大小（字节），一致=成功 |

### 断点续传
- **直连模式（USB/WiFi）**：断线重连后两边对`.progress`偏移量，取最大值继续传。偏移量两边都记，重连后取max。
- **PAW模式**：断线后当前块重传，从这一块继续。
- **直连切PAW**：读`.progress`已下载字节数 ÷ 65536 取整，余数舍去，从对应块继续。电脑跳到对应偏移量重新分块上传，手机端从该块下载拼接。

### 传输期间规则
- 暂停通道升降级探测，传完再恢复
- 用户可手动取消，删除已传的临时文件和`.progress`
- PAW模式下取消调`/paw/delete`清残留块

### PAW中转分块传输
- 块大小：64KB（最后一块可能小于64KB）
- 分块方式：边读边切，读够64KB就上传
- 发送策略：不等待确认，发完立即发下一块
- 暂停/继续：收到暂停指令后发完当前块再停，继续时从下一块开始
- 接收与删除：正常完成传输的块不主动删，靠PAW的10分钟超时自动清理；仅用户取消传输时才调`/paw/delete`删残留块
- 完成通知：手机全部上传完后发送`{"action":"file_complete","file_id":"xxx","total_parts":N}`
- 文件名规则：`{file_id}_part_{序号}`
- PAW并发：一次只传一个文件，全局锁

### PAW流量控制
- 维护计数器：已上传但未被下载删除的块总大小
- 当该值 ≥ 300MB → 暂停上传，手机端阻塞
- 当该值 ≤ 250MB → 恢复上传
- 每上传一块 +64KB，每删除一块 -64KB

---

## 五、完整功能清单（23项，7阶段）

### 第一阶段：通信基础

**功能1：文件传输**
（详见第四节）

**功能2：剪贴板同步**
| 规则项 | 内容 |
|--------|------|
| 监控 | 电脑端每500ms轮询本地剪贴板；手机端无自动监听，需手动点"推送"按钮 |
| 写入 | 收到对方推送 → 直接写入本地系统剪贴板 |
| 防回环 | 消息带源设备ID，收到自己的就忽略 |
| 更新提示 | 手机端Toast提示（未实现悬浮窗）；电脑端系统通知 |
| 历史记录 | 双端各自存本地JSON，上限500条，超出删最旧 |
| 搜索 | 双端各自本地搜索，模糊匹配内容 |
| 来源标注 | 搜索结果每条标"本机"或"手机/电脑" |

**功能3：互传文字**
| 规则项 | 内容 |
|--------|------|
| 发送界面 | 文件名输入框（可选，不填用时间戳命名）+ 长文本输入框 + 发送按钮 |
| 接收界面 | 显示内容，附带"复制"按钮和"保存"按钮 |
| 保存·电脑端 | 拖拽文件图标到任意文件夹 |
| 保存·手机端 | 弹出系统文件夹选择器 |
| 文件格式 | 一律存为.txt |
| 历史记录 | 双端各自存本地，上限100条，超出删最旧 |
| 搜索 | 双端各自本地搜，模糊匹配 |
| 已读/来源标注 | 不做 |

---

### 第二阶段：设备交互

**功能4：查看手机移动路线图**
- 定位不联网，纯GPS，30秒一次
- 位置变化超100米才记录，≤100米忽略
- 无网络时暂存手机本地，一键点上传按钮补传PAW，传完清本地
- 所有轨迹只存PAW，保留7天，超期自动清理
- 电脑和手机都能查看，调在线地图连轨迹线
- 支持按天/7天/自定义时间段筛选
- 查过的数据缓存本地，重复查看不拉PAW
- GPS漂移一次超500公里/分钟的丢弃
- 单条删除、多选删除、一键清空都支持
- 无信号路段灰色虚线标出

**功能5：双向截图**
- **手机截图→电脑**：手机静默截图，相册存一份，自动回传电脑
- **电脑截图→手机**：电脑截图后自动传手机，手机相册存一份，弹窗提示查看
- **触发方式**：电脑点按钮触发，手机端遥控触摸板界面点截屏按钮触发
- **电脑端查看**：弹窗展示，支持笔记本触摸板双指缩放/旋转/单指拖动
- **手机端查看**：触屏手势缩放拖动
- **批注工具**：画笔（红色）、文字批注、箭头、矩形框、马赛克涂抹。点击"批注"按钮进入批注模式，底部出现工具栏选择工具，在图片上绘制
- **批注撤销**：支持撤销上一步，基于操作列表（DrawOp）实现，每次撤销移除最后一个操作
- **保存**：批注后另存为PNG（文件名加_annotated后缀），未批注时保存原图
- **回传**：批注后可将带批注的图片回传电脑

**功能6：手机遥控**
- **模式一·键盘遥控**：上下左右、播放/暂停、音量±、静音、上一首/下一首
- **模式二·完整键盘**：QWERTY全键盘，含Ctrl、Alt、Shift、Win、F1-F12、Enter、退格、方向键。点击修饰键可锁定组合（非长按），再点已锁定键取消锁定。支持多键组合如 Ctrl+Shift+Esc
- **通用**：所有连接方式均可用。USB/WiFi下点击有震动反馈；PAW下延迟大但指令照发。顶部模式切换栏

---

### 第三阶段：屏幕媒体

**功能7：投屏 + 反向控制**
- **手机→电脑**：自研投屏（MediaProjection + ImageReader + VirtualDisplay），非scrcpy。画面+反向控制一体。声音自研传输（AudioPlaybackCaptureConfiguration内录，Android 10+），非sndcpy。仅USB/WiFi可用，PAW不做
- **电脑→手机·USB/WiFi**：推流（mss截屏 + MJPEG），手机解码显示
- **电脑→手机·PAW**：未实现（PAW通道已禁用）
- **操控方式**：合并预览与操控，始终可操作。ADB模式：down记录位置，up执行tap（非移动才触发）；WiFi模式：发送touch事件。无障碍服务未连接时Toast提示
- **音量控制**：首次触摸立即发送，拖动中50ms节流，松手发送最终值
- **通用**：投屏帧率最高60fps，全屏时保持原始宽高比。窗口关闭时发送mirror_stop和pc_stream_stop给对端

**功能8：共享摄像头**
- 手机摄像头画面实时传电脑
- 可选前置/后置/切换
- 分辨率固定720p（已移除分辨率下拉选择）
- 独立窗口显示，关闭时发送camera_stop并清除帧缓存
- 仅USB和WiFi可用，PAW禁用

**功能9：电脑播放手机声音**
- 自研声音传输，非sndcpy
- Android 10+使用AudioPlaybackCaptureConfiguration内录系统音频；不支持时降级为麦克风录音
- PCM无损传输，5个buffer批量发送（约100ms）减少HTTP开销
- 需先启动投屏获取MediaProjection授权后才能内录
- 仅USB和WiFi可用，PAW禁用

---

### 第四阶段：系统读取

**功能10：通知读取**
- 手机监听所有应用通知，推电脑（需授权通知读取权限）
- 通知权限获取时机：双端任意一端首次进入通知界面时才弹出权限请求，应用启动时不请求。已开启权限则不再跳转设置页
- 电脑端程序内弹窗显示完整通知，包含手机通知的所有功能按钮（如回复、已读、删除等），点击哪个执行哪个
- 电脑端可删除手机上的通知，本地历史记录保留不删
- 黑名单过滤：过滤指定App的通知（非白名单，方向相反但效果等价）
- 单击通知 → 弹出选项：是否打开对应手机应用并开始投屏
- 通知历史本地保存，上限200条，可搜索
- 仅USB/WiFi可用，PAW不推实时通知
- 手机端通知的所有功能按钮全部保留，点击哪个就执行哪个

---

### 第五阶段：文件数据

**功能14：远程文件管理**
- 电脑端浏览手机目录结构
- 支持：删除、重命名、新建文件夹、下载手机文件到电脑、上传电脑文件到手机
- 传输复用功能1规则：单任务、自动选通道、断点续传、校验大小
- 管理操作走当前最优通道发指令

**功能17：APK安装**
- 电脑端拖入apk → ADB push → ADB静默安装
- 仅ADB可用，WiFi和PAW下该功能置灰
- 电脑端显示传输进度，手机端弹出"正在安装"通知不可手动移除，安装完成后自动消失
- 静默安装，不弹系统安装界面，无需手机端手动确认

---

### 第六阶段：高级功能

**功能18：双向状态 + 进程管理**
- **手机→电脑**：电量、温度、网络类型、存储空间、内存占用。PC端仪表盘显示手机电量(%)、温度(°C)、网络类型(WiFi/移动数据/以太网)
- **电脑→手机**：内存占用、存储空间、网络状态（CPU显示已移除）
- 所有连接方式都可用，手机端状态每5秒上报一次，连接成功时立即上报
- 连接延迟实时显示（RTT毫秒数，显示在连接状态文字中）
- 手机端可查看电脑进程列表，支持搜索
- 手机端长按进程可结束选中进程（PC端发送taskkill /PID /F，手机端发送kill命令）
- 手机端可以管理员权限启动新程序（电脑端需以管理员身份运行本软件；Ubuntu下不用sudo）

**功能20：剪贴板历史**
- 双端各自查看所有同步过的剪贴板记录
- 每条标注来源：本机 / 对方
- 点一条直接复制到系统剪贴板
- 上限500条，超出删最旧
- 双端各自本地搜索

**功能21：手机已安装应用管理**
- 电脑端列表显示手机所有应用：应用名、包名、版本号、大小、安装时间
- WiFi模式：手机端通过PackageManager获取应用名/包名/版本/APK大小(File(sourceDir).length())/安装时间(firstInstallTime)，JSON返回PC端
- ADB模式：`pm list packages -3 -f`获取包名+APK路径，`dumpsys package`解析版本+安装时间，`wc -c`获取APK大小。限制最多200个应用，每个dumpsys有15s超时
- 支持按应用名或包名搜索
- 卸载：ADB先备份APK到电脑 → 再ADB卸载 → 备份存为`应用名_版本号.apk`
- 清除数据：ADB执行`pm clear 包名`
- 导出APK：ADB pull到电脑
- 仅ADB可用，WiFi和PAW下该功能置灰

**功能22：电脑电源管理**
- 操作：关机、重启、休眠、锁定屏幕
- 关机/重启有30秒倒计时，休眠/锁定立即执行
- 不需要确认，手机端点按钮直接开始倒计时
- 双端同时弹窗显示倒计时进度条
- 电脑端和手机端均可点取消中断倒计时
- 所有连接方式均可用

**功能23：剪贴板收藏**
- 剪贴板历史中每条可标记为收藏，收藏条目独立存储
- 收藏上限50条，超出提示"收藏已满，请先取消部分收藏"
- 收藏条目不受500条历史上限清理影响，永久保留
- 收藏同步到对端，双端收藏保持一致
- 可随时取消收藏，退回普通历史
- 收藏和普通历史可分别搜索

---

### 第七阶段：效率工具

**功能J：推送网页**
- 手机端选中文字发送 → 电脑端Edge自动打开该文字（URL则直接打开，普通文字则用搜索引擎搜索）
- 电脑端发给手机：ADB连接下，手机自动复制内容 → ADB控制Via浏览器打开（URL直接开，普通文字搜索）；非ADB连接下，仅复制到手机剪贴板
- 已移除"本机Via浏览器打开"按钮
- 未实现：长按菜单固定、Edge自动打开开关

---

## 六、已砍功能汇总

| 原编号 | 功能 | 砍掉原因 |
|--------|------|---------|
| 4（旧） | 找手机 | 用处不大 |
| 录屏 | 录屏 | 不如系统原生录屏 |
| 陀螺仪鼠标 | 手机遥控·实验功能 | 不好用 |
| 11 | 短信读取+发送 | 短信通知走通知读取即可 |
| 12 | 通话记录读取 | 基本不通话 |
| 13 | 通讯录同步 | 不需要 |
| 15 | 相册同步 | 图片太多 |
| 16 | 音乐同步 | 约等于通知+播放，不需要 |
| 19 | 应用列表读取 | 与进程管理重复 |
| 共享麦克风 | 共享麦克风 | 不需要 |
| A | 电脑看手机摄像头 | 与功能8重复 |
| N | 音量混音器 | 太复杂 |

---

## 七、通信架构补充

### cmd.json拆分（纯ADB模式）
- 原`cmd.json`拆成两个单向文件：
  - `cmd_pc_to_phone.json` → 电脑只写，手机只读
  - `cmd_phone_to_pc.json` → 手机只写，电脑只读
- 文件名规定方向，无需额外锁文件

### 连接切换时的传输处理
- **指令层**：指令轻量，切换时不等待，发完再切。设置2秒切换等待窗口，2秒内没发完强制丢弃，切通道后重发
- **文件层**：切换信号来后发完当前块就停，记录已传偏移量到`.progress`，切通道后从断点继续
- **队列层**：切换期间队列锁住，不塞新任务，切完原样搬走
- **降级**：10秒内完成切换
- **升级**：可等30秒，尽量排空队列再切

### file_complete丢包处理
- 接收方收到`file_complete`后立即回复ack（同样`{"action":"file_complete","file_id":"xxx"}`）
- 发送方10秒内没收到ack就重发
- 收到ack后本地`.progress`删除，传输结束

### 剪贴板电脑端监听
- 统一用轮询方式（每500ms读系统剪贴板对比上次内容）
- Linux用`xclip`或`wl-paste`配合轮询
- Windows用`pyperclip`或`win32clipboard`配合轮询
- 不依赖系统事件

---

## 八、安全
- 硬编码Secret Token
- 所有PAW请求头必须带上Token
- Token不对直接返回403

---

## 九、手机端后台保活
- 前台服务 + 常驻通知
- 申请电池优化白名单（引导用户手动加）
- 无障碍服务权限（提升进程优先级）
- WAKE_LOCK保CPU唤醒
- 软件内设置"永不休眠"开关（当前硬编码FLAG_KEEP_SCREEN_ON常亮，未提供用户可切换开关）

---

## 十、数据生命周期
- 所有交互记录完整保留
- 超过180天的旧记录自动删除（定时任务）**注：该逻辑在PAW端实现，PAW禁用后本地无180天清理**
- PAW文件块：下载后不主动删，超时未下载（10分钟）自动清理
- 仅取消传输时才调`/paw/delete`删残留块

---

## 十一、开发环境

### 电脑端代码模块
**核心框架**：Flask（Web服务器）、Flask-SocketIO（WebSocket实时推送）

**通信层**：subprocess（调ADB）、requests（HTTP请求）、socket（UDP广播）、pyperclip（剪贴板）

**系统控制**：pyautogui（模拟键鼠）、PIL/Pillow（截图处理）、mss（屏幕捕获）

**硬件/媒体**：OpenCV（摄像头）、PIL/Pillow（截图处理）、mss（屏幕捕获）、pyaudio（音频播放/采集）

**工具/存储**：json（配置）、sqlite3（通知历史/传输记录）、threading（后台任务）、os/shutil（文件操作）

**前端**：HTML+JS+Socket.IO JS+Leaflet.js（地图）

### 手机端开发环境搭建（Windows）
1. 装JDK 11（Adoptium Temurin 11，.msi安装）
2. 下载Android SDK Command-line Tools（zip解压到`C:\android-sdk\cmdline-tools\latest\`）
3. 配置环境变量：`ANDROID_HOME=C:\android-sdk`，PATH追加`cmdline-tools/latest/bin`和`platform-tools`
4. sdkmanager装组件：`platform-tools`、`platforms;android-29`、`build-tools;29.0.3`
5. 创建项目目录结构：PhoneHub/app/src/main/java/com/phonehub/ + res/ + AndroidManifest.xml
6. 编写Gradle配置（settings.gradle.kts、根build.gradle.kts、app/build.gradle.kts、gradle-wrapper.properties）
7. 获取Gradle Wrapper（先装Gradle 7.5本体，执行`gradle wrapper --gradle-version 7.5`）
8. Trae打开项目，终端执行`gradlew.bat assembleDebug`
9. APK输出在`app/build/outputs/apk/debug/app-debug.apk`

### 手机端核心依赖库
**网络**：Ktor Client、Ktor Server (Netty)
**序列化**：kotlinx.serialization
**异步**：kotlinx.coroutines
**系统**：NotificationListenerService、AccessibilityService、DevicePolicyManager、PackageManager、ClipboardManager、LocationManager、SensorManager
**媒体**：CameraX、MediaProjection、AudioRecord
**UI**：Jetpack Compose、Coil、Osmdroid
**保活**：Foreground Service、WakeLock、AlarmManager

---

## 十二、开发路线

按阶段推进：通信基础 → 设备交互 → 屏幕媒体 → 系统读取 → 文件数据 → 高级功能 → 效率工具

推荐编码顺序：
1. 手机端+电脑端：ConnectionManager（探测、连接、切换）
2. 通信层：Channel抽象 + WiFi直连
3. 功能1（文件传输）：验证通道切换+断点续传
4. 逐功能叠加

---

## 十三、代码架构

### 整体分层（三层）
| 层 | 手机端 | 电脑端 | PAW |
|----|--------|--------|-----|
| 通信层 | ConnectionManager | ConnectionManager | Flask路由 |
| 业务层 | 各功能模块 | 各功能模块 | 数据中转/清理 |
| UI层 | Compose界面 | Flask本地Web UI | 无 |

### 跨平台通信抽象
```
业务层
  ↓
ChannelManager（统一接口）
  ├─ AdbChannel（USB数据线）
  ├─ WiFiChannel（路由器直连）
  └─ PawChannel（PAW中转）
```
切换通道时上层无感知，ChannelManager内部处理升降级。

### 连接管理器（双方都有）
```
ConnectionManager
├─ 探测线程（每5秒检测各通道可用性）
├─ 通道选择器（优先级：USB > WiFi > PAW）
├─ 升降级控制器（升级等3次确认，降级立即切）
└─ 心跳/重连（断线重试3次，失败降级）
```

### 核心模块复用
| 模块 | 被哪些功能复用 |
|------|---------------|
| 文件传输引擎 | 功能1/5/14/17/21 |
| 剪贴板监控 | 功能2/20/23 |
| 通知监听 | 功能10 |
| 投屏引擎(自研MediaProjection) | 功能7/9 |

### 数据流向
```
手机 ←→ 电脑：直连时双向HTTP，ADB时双向文件轮询
手机 ←→ PAW ←→ 电脑：中转时长轮询+分块上传
```

---

## 附录：功能编号对照表

| 最终编号 | 功能名 | 阶段 |
|---------|--------|------|
| 1 | 文件传输 | 一·通信基础 |
| 2 | 剪贴板同步 | 一·通信基础 |
| 3 | 互传文字 | 一·通信基础 |
| 4 | 查看手机移动路线图 | 二·设备交互 |
| 5 | 双向截图 | 二·设备交互 |
| 6 | 手机遥控 | 二·设备交互 |
| 7 | 投屏+反向控制 | 三·屏幕媒体 |
| 8 | 共享摄像头 | 三·屏幕媒体 |
| 9 | 电脑播放手机声音 | 三·屏幕媒体 |
| 10 | 通知读取 | 四·系统读取 |
| 14 | 远程文件管理 | 五·文件数据 |
| 17 | APK安装 | 五·文件数据 |
| 18 | 双向状态+进程管理 | 六·高级功能 |
| 20 | 剪贴板历史 | 六·高级功能 |
| 21 | 手机已安装应用管理 | 六·高级功能 |
| 22 | 电脑电源管理 | 六·高级功能 |
| 23 | 剪贴板收藏 | 六·高级功能 |
| J | 推送网页 | 七·效率工具 |

当前功能总数：**18项**（含功能J推送网页）

---

## 关键逻辑修正记录（按讨论顺序）

1. **直连切PAW不重传**：读`.progress`已下载字节数÷65536取整，余数舍去，从对应块继续
2. **cmd.json拆分为两个单向文件**：`cmd_pc_to_phone.json`和`cmd_phone_to_pc.json`，文件名规定方向
3. **剪贴板历史500条上限**，双端各自本地搜索
4. **互传文字加保存功能**：电脑端拖拽保存，手机端选文件夹；历史100条
5. **找手机功能砍掉**，换成查看手机移动路线图
6. **截图功能扩展为双向截图**，加批注功能，支持笔记本触摸板手势
7. **录屏功能砍掉**
8. **手机遥控砍掉触控板模式和PPT模式**，改为键盘遥控+完整键盘
9. **陀螺仪鼠标砍掉**
10. **投屏用scrcpy，声音用sndcpy**，USB自动tcpip
11. **电脑→手机PAW投屏：1fps，JPEG 60%**
12. **共享麦克风砍掉**
13. **通知读取砍掉Windows系统通知**，改用程序内弹窗保留通知所有功能按钮
14. **短信、通话记录、通讯录砍掉**
15. **远程文件管理加入传输功能**
16. **相册同步、音乐同步砍掉**
17. **APK安装改为静默安装**，仅ADB可用
18. **双向状态加进程管理**：手机可查看/结束电脑进程，管理员权限启动程序
19. **电脑电源管理**：关机/重启30秒倒计时，双端进度条可取消
20. **剪贴板收藏**：收藏同步到对端，双端一致
21. **推送网页**：长按菜单固定，ADB下自动开Via，非ADB仅复制
22. **文件传输期间暂停通道切换探测**
23. **file_complete加ack机制**
24. **传输取消时才清PAW残留块**
25. **剪贴板电脑端用轮询方式**
26. **应用卸载前ADB备份APK到电脑**

遗漏的结构性决策
手机端不做 HTTP Server：纯 WiFi 模式下电脑永远是 Server，手机是 Client，手机主动连电脑 Flask 端口。这个简化了架构，之前总结里没说清楚。

剪贴板冲突不用时间戳：手机和电脑时钟不同步，时间戳比新旧不可靠。改为两端各自轮询对比上一次内容，变了就推。总结里写了轮询，但没提为什么不用时间戳。

遗漏的边界规则
文件同名冲突：传输时目标路径已有同名文件，弹窗让用户选覆盖/重命名/跳过。文件传输那节漏了这个。

文件传输完成后的"快捷打开"弹窗：用户讨论时提过一嘴"快捷打开这个弹窗可以保留"，后来虽然简化了确认逻辑，但这个弹窗没说不做。是否保留待定。

遗漏的环境/测试相关
PythonAnywhere 免费账户压力测试：讨论过需要先测长轮询稳定性（是否频繁 429）、64KB 块上传延迟、并发连接上限。这些属于开发前置验证，不是功能规则，但应该记一笔。

Ubuntu 下剪贴板监听方式：Linux 用 xclip 或 wl-paste 轮询，Windows 用 pyperclip。总结里电脑端模块用了 pyperclip，没区分平台。

apk编译
**当前环境配置总览**

---

## 开发环境

| 组件 | 版本/路径 |
|------|----------|
| 操作系统 | Windows 10 |
| JDK | OpenJDK 11.0.31 (Temurin) |
| Gradle | 7.5 (通过 wrapper 本地引用) |
| Android Gradle Plugin | 7.4.2 |
| Kotlin | 1.8.22 |
| compileSdk | 30 (Android 11) |
| minSdk | 29 (Android 10) |
| targetSdk | 30 |
| Build-Tools | 30.0.3 |

---

## 十四、实现与设计差异记录

以下为实际开发中与原始设计文档的差异，按功能列出：

### 投屏方案变更
- 原设计：调用scrcpy/sndcpy
- 实际实现：自研 MediaProjection + ImageReader + VirtualDisplay（画面），AudioPlaybackCaptureConfiguration（声音内录，Android 10+），降级麦克风
- 原因：不依赖第三方工具，减少外部进程管理复杂度

### 投屏模式变更
- 原设计：预览模式（仅看）+ 操控模式（点击进入）
- 实际实现：合并为始终可操控，无模式切换

### 修饰键锁定方式变更
- 原设计：长按修饰键锁定多键组合
- 实际实现：点击修饰键切换锁定/解锁状态

### CPU显示移除
- 手机端首页和设置页已移除CPU占用显示
- 保留连接延迟(RTT)显示替代

### 通知过滤方向变更
- 原设计：白名单过滤（只推指定App）
- 实际实现：黑名单过滤（过滤指定App），效果等价

### 通知权限获取时机变更
- 原设计：应用启动时获取
- 实际实现：双端任意一端首次进入通知界面时才请求，应用启动不请求

### 摄像头分辨率变更
- 原设计：可选480p/720p/1080p
- 实际实现：固定720p，移除分辨率下拉

### 亮度控制移除
- 已从投屏页面移除亮度滑块

### 鼠标光标移除
- 已移除PC端投屏画布的光标显示
- 已移除手机端PC光标位置显示

### 文件管理增强（新增）
- 目录优先排序
- 文件类型emoji图标（📁📦🖼🎬🎵📄📝🐍📎等）
- 右键菜单"复制到..."功能

### 进程管理增强（新增）
- 长按进程弹出确认对话框，支持结束进程
- PC端：taskkill /PID /F
- 手机端：kill命令

### 文字消息去重（新增）
- 维护"已处理内容"集合（handledTextContents）
- 弹窗关闭/复制后5秒内相同内容不再弹出

### 音频批量发送（新增）
- 累积5个buffer（约100ms）合并后一次性发送，减少HTTP请求开销

### 音量节流（新增）
- 首次触摸立即发送当前值
- 拖动中50ms节流
- 松手发送最终值

### 后台资源清理（新增）
- 手机端切页时停止旧页面任务（投屏/摄像头/通知/文件管理）
- PC端投屏窗口关闭时发送mirror_stop和pc_stream_stop
- PC端摄像头窗口关闭时发送camera_stop并清除帧

### PAW通道状态
- PC端PAW通道代码全部注释禁用（CHANNEL_PAW、_check_paw、_start_paw_polling等）
- pythonanywhere/app.py中转服务代码存在但未启用
- 当前仅支持USB(ADB) + WiFi双通道

## 项目结构

```
C:\PhoneHub\
├── app\
│   ├── build\                          ← 构建输出（含 APK）
│   └── src\main\
│       ├── java\com\phonehub\
│       │   └── MainActivity.kt         ← 唯一源码
│       ├── res\
│       │   └── values\
│       │       └── styles.xml           ← 主题
│       └── AndroidManifest.xml
├── gradle\
│   └── wrapper\                         ← Gradle Wrapper 文件
├── settings.gradle.kts
├── build.gradle.kts                     ← 根构建脚本
├── gradle.properties                    ← android.useAndroidX=true
├── gradlew.bat                          ← 构建入口
└── local.properties                     ← sdk.dir
```
