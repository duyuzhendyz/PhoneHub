// LiveMap · 独立测试工程（**不并入 PhoneHub 主工程**）
// 版本号与主工程保持一致，以便复用本机 Gradle 缓存（可离线构建）。
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.23" apply false
}
